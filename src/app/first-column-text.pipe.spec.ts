import { FirstColumnTextPipe } from './first-column-text.pipe';

describe('FirstColumnTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstColumnTextPipe();
    expect(pipe).toBeTruthy();
  });
});
