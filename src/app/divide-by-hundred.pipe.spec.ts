import { DivideByHundredPipe } from './divide-by-hundred.pipe';

describe('DivideByHundredPipe', () => {
  it('create an instance', () => {
    const pipe = new DivideByHundredPipe();
    expect(pipe).toBeTruthy();
  });
});
